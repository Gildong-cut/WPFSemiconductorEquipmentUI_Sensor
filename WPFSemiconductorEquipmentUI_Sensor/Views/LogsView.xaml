<UserControl x:Class="WPFSemiconductorEquipmentUI_Sensor.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WPFSemiconductorEquipmentUI_Sensor.Controls">
    <StackPanel>
        <DockPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Activity &amp; Sensor Logs" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="{Binding Description}" Style="{StaticResource BodyText}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <controls:StatusBadge Text="SQLite SYNC" Tone="Normal" Margin="0,0,8,0" />
                <controls:StatusBadge Text="AUDIT TRAIL" Tone="Blue" Margin="0,0,8,0" />
                <controls:StatusBadge Text="SENSOR / AUDIT" Tone="Blue" />
            </StackPanel>
        </DockPanel>

        <Border Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="114" />
                    <ColumnDefinition Width="114" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="126" />
                    <ColumnDefinition Width="112" />
                    <ColumnDefinition Width="*" MinWidth="150" />
                    <ColumnDefinition Width="104" />
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="From" Style="{StaticResource CaptionText}" />
                    <TextBox Text="2026-05-28" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="To" Style="{StaticResource CaptionText}" />
                    <TextBox Text="2026-05-28" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="User" Style="{StaticResource CaptionText}" />
                    <ComboBox SelectedIndex="0">
                        <ComboBoxItem Content="All users" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="Type" Style="{StaticResource CaptionText}" />
                    <ComboBox SelectedIndex="0">
                        <ComboBoxItem Content="All events" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="0,0,10,0">
                    <TextBlock Text="Severity" Style="{StaticResource CaptionText}" />
                    <ComboBox SelectedIndex="0">
                        <ComboBoxItem Content="All" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="5" Margin="0,0,10,0">
                    <TextBlock Text="Search" Style="{StaticResource CaptionText}" />
                    <TextBox Text="pressure, shutdown..." />
                </StackPanel>
                <Button Grid.Column="6"
                        Content="EXPORT"
                        Style="{StaticResource CompactButtonStyle}"
                        Width="104"
                        VerticalAlignment="Bottom" />
            </Grid>
        </Border>

        <UniformGrid Columns="4" Margin="0,16,0,0">
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="TOTAL LOGS" Tone="Blue" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding TotalLogCount}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="Loaded SQLite records" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="WARNINGS" Tone="Warning" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding WarningCount}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="Warnings and blocked actions" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <controls:StatusBadge Text="RISK EVENTS" Tone="Danger" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding RiskCount}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="Shutdown-related events" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" MinHeight="124" Padding="16">
                <StackPanel>
                    <controls:StatusBadge Text="LATEST EVENT" Tone="Normal" HorizontalAlignment="Left" />
                    <TextBlock Text="{Binding LastEventTime}" Margin="0,10,0,0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                    <TextBlock Text="Most recent saved record" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Margin="0,18,0,0" MinHeight="472">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="600" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="276" />
            </Grid.ColumnDefinitions>

            <TabControl>
                <TabItem Header="Sensor Values">
                    <DataGrid ItemsSource="{Binding SensorLogs}" SelectedItem="{Binding SelectedLog}" AlternationCount="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="82" />
                            <DataGridTextColumn Header="Event" Binding="{Binding Event}" Width="*" />
                            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="82" />
                            <DataGridTextColumn Header="Saved" Binding="{Binding Saved}" Width="66" />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="User Actions"
                         Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DataGrid ItemsSource="{Binding UserActionLogs}" SelectedItem="{Binding SelectedLog}" AlternationCount="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="82" />
                            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="90" />
                            <DataGridTextColumn Header="User" Binding="{Binding User}" Width="100" />
                            <DataGridTextColumn Header="Event" Binding="{Binding Event}" Width="*" />
                            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="82" />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Selected Log Detail" Style="{StaticResource SectionTitleText}" />
                    <controls:StatusBadge Text="{Binding SelectedLog.Severity}" Tone="Blue" HorizontalAlignment="Left" Margin="0,14,0,0" />
                    <TextBlock Text="{Binding SelectedLog.Event}"
                               Margin="0,14,0,0"
                               Style="{StaticResource BodyText}"
                               TextWrapping="Wrap" />
                    <Border Margin="0,16,0,0" Padding="12" CornerRadius="8" Background="{StaticResource BrushSurfaceSubtle}">
                        <StackPanel>
                            <TextBlock Text="Audit Detail" Style="{StaticResource BodyStrongText}" />
                            <DockPanel Margin="0,12,0,0"><TextBlock Text="Time" Style="{StaticResource CaptionText}" /><TextBlock DockPanel.Dock="Right" Text="{Binding SelectedLog.Time}" Style="{StaticResource BodyStrongText}" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="Source" Style="{StaticResource CaptionText}" /><TextBlock DockPanel.Dock="Right" Text="{Binding SelectedLog.Source}" Style="{StaticResource BodyStrongText}" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="User" Style="{StaticResource CaptionText}" /><TextBlock DockPanel.Dock="Right" Text="{Binding SelectedLog.User}" Style="{StaticResource BodyStrongText}" /></DockPanel>
                            <DockPanel Margin="0,10,0,0"><TextBlock Text="Saved" Style="{StaticResource CaptionText}" /><TextBlock DockPanel.Dock="Right" Text="{Binding SelectedLog.Saved}" Style="{StaticResource BodyStrongText}" /></DockPanel>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Saved in SQLite / activity_logs" Margin="0,14,0,0" Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
